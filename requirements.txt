flask>=3.0
flask-socketio>=5.3
gunicorn>=21.0
gevent>=23.0
gevent-websocket>=0.10
kubernetes>=28.0
google-genai>=1.0
google-auth>=2.0
pyyaml>=6.0
requests>=2.31
# NOTE: httpx and fastmcp are NOT included here — they conflict with gevent
# monkey.patch_all() and corrupt the kubernetes client's urllib3 connections.
# Confluence MCP uses the raw HTTP JSON-RPC fallback (requests) instead.
# If you need fastmcp, install it separately: pip install fastmcp httpx
