apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: {{ template "application.name" . }}-jwt-auth
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ $.Release.Name }}
    chart: {{ $.Chart.Name }}
    release: "{{ $.Release.Name }}"
spec:
  selector:
    matchLabels:
      {{- toYaml .Values.appSelector.matchLabels | nindent 6 }}
  jwtRules:
    - issuer: {{ .Values.virtualservice.cidp_oauth2_base_url }}
      jwksUri: {{ .Values.virtualservice.cidp_oauth2_base_url }}/connect/jwk_uri
      forwardOriginalToken: true
      {{- if or .Values.oicdAudience .Values.virtualservice.audiences }}
      audiences:
        {{- if .Values.oicdAudience }}
        # Per-app audience — this app's own OIDC client ID.
        - {{ .Values.oicdAudience }}
        {{- end }}
        {{- range .Values.virtualservice.audiences }}
        # Shared audience — accept tokens for multiple apps in the namespace.
        - {{ . }}
        {{- end }}
      {{- end }}
      outputClaimToHeaders:
        - header: Remote-User
          claim: email